* 完成transit状态类：
	1. 找到适合任务的发布主题。
	2. 由于有限状态机的执行局限，暂时设置align状态类入口在transit状态执行过程中。
* 在mavros_interface.py中补充姿态设计。
* 关于状态类中的时间戳问题
* 关于主循环中的看门狗问题
* 启动脚本
* 这是一个ROS工作空间，里面已经有了一个基础的仿真代码，我的四旋翼仿真技术栈是：PX4 v1.16.1，GazeboC11，ROS1，YOLOv8，我的最终目的是仿真PX4 Offboard模式下的代码，运行一个状态机循环，最终任务过程是起飞状态之后切换至悬停状态，之后切换至transit状态在一个矩形场地内进行遍历航线飞行，过程中实时监听YOLO节点发布的主题，直到YOLO检测到目标，切换到drop状态，精准靠近目标并且驱动舵机投放，之后切换状态返回home点。现在我首先需要你完成一个小目标：实现起飞-悬停-遍历-返回，即不包括YOLO和相关任务的代码；过程中每完成一个小部分并且测试通过之后在git dev分支上提交；将代码改动详细说明信息生成放在src/dev.md中
